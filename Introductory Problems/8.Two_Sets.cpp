#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
#define ll long long 
static const int mod = 1e9 + 7;
int main()
{
    ios::sync_with_stdio(false), cin.tie(0);
    
    int n;
    cin >> n;
    vector<int> a,b;    
    ll sum = n*(n+1)/2;
    if(sum%2!=0) {
        cout<<"NO"; 
        
    }else{
        for(int i=0;i<n;i++){
            
        }
    }
    cout<<ans<<endl;

    return 0;
}

